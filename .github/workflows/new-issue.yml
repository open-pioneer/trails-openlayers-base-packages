name: Add new issues to maintenance board

on:
  issues:
    types:
      - opened
      - reopened
      - transferred

permissions:
  contents: read

jobs:
  add-to-board:
    name: Add issue to board
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Assign to board
        uses: open-pioneer/trails-repo-automation/.github/actions/add-to-maintenance-board@3e0709264e6cb50fb00af3bf97d0b05bd0024591 # main
        with:
          github-token: ${{ secrets.ISSUE_AUTOMATION_TOKEN }}
