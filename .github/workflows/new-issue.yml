name: Add new issues to maintenance board

on:
  issues:
    types:
      - opened
      - reopened
      - transferred

permissions:
  contents: read

jobs:
  add-to-board:
    name: Add issue to board
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Assign to board
        uses: open-pioneer/trails-repo-automation/.github/actions/add-to-maintenance-board@885300a1d435f5341fe732c0cef9351c45c615b7 # main
        with:
          github-token: ${{ secrets.ISSUE_AUTOMATION_TOKEN }}
