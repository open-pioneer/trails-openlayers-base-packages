---
"@open-pioneer/search": minor
---

Add support for an `id` property on the search source interface to identify sources. The property remains optional for the time being to preserve backwards compatibility.
