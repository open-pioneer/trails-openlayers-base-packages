---
"@open-pioneer/toc": minor
---

- WMS capabilities are now validated: a configured sublayer name that does not exist in the service's capabilities is visible in the parent layer.
- Problem indicators distinguish a layer's own error (red, checkbox disabled) from a child error bubbling up to a group (orange, group checkbox stays enabled so it can still be toggled).
