---
"@open-pioneer/map-ui-components": minor
---

**Breaking:** Rename `<ToolButton />` props for consistency with Chakra naming conventions:

- `isLoading` (old) --> `loading` (new)
- `isActive` (old) --> `active` (new)
- `isDisabled` (old) --> `disabled` (new)
